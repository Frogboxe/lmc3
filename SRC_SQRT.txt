
SET 0;
STA COUNTER;
SET 1;
STA TEMP;
SET 1;
STA ONE;
SET 2;
STA TWO;

INP;
STA SQRT;

LOOP;

LDA SQRT;
SUB TEMP;
STA SQRT;

LDA TEMP;
ADD TWO;
STA TEMP;

LDA COUNTER;
ADD ONE;
STA COUNTER;

LDA SQRT;
BRP LOOP;

LDA COUNTER;
SUB ONE;
OUT;

HLT;
